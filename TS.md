# Technical Specification

Implement a plugin for the Visual Studio Code development environment for the C++ language with the following functionality:
- generation of templates for conditional statements `if`, `else`, `if else` using keyboard shortcuts
- generation of cycle patterns `while`, `for` using keyboard shortcuts